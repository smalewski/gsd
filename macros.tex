% Dynamic versions of these commands are used in the
% web server to hide or show evidences and types in ascriptions.

% This version always shows evidences and types.
\newcommand{\ascParens}[1]{({#1})}
\newcommand{\ascription}[3]{{#1} ~ {#2} ~ : ~ {#3}}
